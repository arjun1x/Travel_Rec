# Use npm run demo for the local sample collection. Live mode is the default.
VITE_DEMO_MODE=false
# Dev proxy only. Do not put server secrets in VITE_ variables.
API_PROXY_TARGET=http://localhost:8000
